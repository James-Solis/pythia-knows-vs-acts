# Exact versions from the environment the investigation ran in (Python venv on
# Windows + WSL-style CUDA, single RTX 4070). Install torch FIRST from the CUDA
# index, then the rest:
#
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# (The installed torch build was 2.5.1+cu121; the +cu121 local tag only resolves
# from the PyTorch index above, not from default PyPI.)
torch==2.5.1
transformer-lens==3.2.1
transformers==5.8.0
pandas==3.0.3
matplotlib==3.10.9
numpy==2.4.4
